<%= form_with(model: ticket_order, local: true) do |form| %>
  <% if ticket_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_order.errors.count, "error") %> prohibited this ticket_order from being saved:</h2>

      <ul>
      <% ticket_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :gig_id %>
    <%= form.text_field :gig_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
